spring:
  application:
    name: EUREKA-SERVER
server:
  port: 8761

#eureka:#  client:#    register-with-eureka: false
#    fetch-registry: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false